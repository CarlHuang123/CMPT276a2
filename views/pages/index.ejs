<!DOCTYPE html>
<html>
<head>
  <%- include ("../partials/header.ejs") %>
</head>

<body>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Name</th>
      </tr>
    </thead>
    <tbody>
      <% for(var i=0; i < rectangles.length; i++) { %>
        <tr>
          <td><%= rectangles[i].id %></td>
          <td><a href="/<%= rectangles[i].id %>"><%= rectangles[i].name %></a></td>
        </tr>
     <% } %>
    </tbody>
  </table>
  <br>
  <center>
    <p>
      <b>You can do this by clicking on the name of the unwanted data, it will lead you to a page that shows all the information, with a delete button below. click the button to delete</b> 
    </p>
  </center>

</body>
</html>
